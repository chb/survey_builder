<SelectMultipleQuestion<% if (lineitem.about){ %>rdf:about="<%= lineitem.about %>" <% } %>>
    <% if (lineitem.questionText) { %>
        <questionText><%= lineitem.questionText %></questionText>
    <% } %>
    <% if (lineitem.answerProperty) { %>
        <answerProperty rdf:resource="<%= lineitem.answerProperty %>" />
    <% } %>
    <% if (lineitem.defaultAnswerForEstimation) { %>
        <defaultAnswerForEstimation rdf:resource="<%= lineitem.defaultAnswerForEstimation %>" />
    <% } %>
    <questionAnswers>
        <AnswerSequence>
            <% if (lineitem.childId) {%>
                <% var childLineitem = Lineitem.findOne({id:lineitem.childId}) %>
                <%= $.View('//surveybuilder/views/lineitem/show_rdf', {lineitem:childLineitem}) %>
            <% } %>
        </AnswerSequence>
    </questionAnswers>
</SelectMultipleQuestion>


